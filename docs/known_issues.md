# Known Issues

## 当前遗留问题

- 当前版本为 UI Preview Mode，所有业务功能均为占位。
- 旧的数据库、采集、解析、事件识别等模块仍保留在源码中，作为后续真实功能实现参考；主窗口不再调用这些模块。
- 历史自动化测试已切换为预览模式烟测，不再验证旧业务链路。

## 暂未实现功能

- 真实公司搜索。
- 真实联网数据源。
- 真实爬虫。
- 真实数据库业务写入。
- 自选公司持久化。
- 公司详情数据。
- 风险规则。
- AI 总结。
- 导出报告。

## 未确认文件

- `installer/` 和 PyInstaller 配置保留，用于后续桌面打包。
- `src/cdm_desktop/db/`、`connectors/`、`parsing/`、`event_engine/` 保留为未来业务实现参考；当前 UI 不调用。
- `.pytest_cache/` 在本次清理时被 Windows 拒绝访问；它是本地生成缓存，不包含产品数据，后续可在关闭相关进程或提升权限后删除。
